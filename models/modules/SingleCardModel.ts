export type SingleCardProps = {
    item:SingleCardItem,
}
export type SingleCardItem = {
    id:number,
    title:string,
    img: string,
    roomCount: number,
    meterage: number,
    price: number,
    code:string,
    desc: string,
}